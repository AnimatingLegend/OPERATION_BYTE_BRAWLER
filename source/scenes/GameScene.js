/* -----------------------------------------------*
*                   Game.js                       |
*         Where the magic (also) happens          |
*------------------------------------------------*/
export class GameScene {
     constructor(game) {
          this.game = game;
          this.context = game.context;
     }
}